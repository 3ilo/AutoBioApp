Generate a single simple, abstract illustration of: {{elementDescription}}. This image will be reused across all frames, so it should be stable, neutral, and not imply motion.

Style: flat 2D, minimal detail, soft muted palette (pastels), no text. Same flipbook style—clean, calm, suggestive rather than literal. No background; the element should be clearly visible and usable as a reference asset. It should include imperfections to ground it more in reality, but those imperfections should look the same whenever this element is reused.
